git config --global user.name "username"
git config --global user.email "email"
git config --global user.email
git config --global user.email


git init

git add .

git commit -m "Commit Name"

git remote add origin git@github.com:iushsui/Python-Class.git